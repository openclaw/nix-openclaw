name: Yolo Update Pins

on:
  workflow_dispatch: {}

permissions:
  actions: read
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Recovery freeze
        run: echo "Yolo Update Pins is disabled during release recovery."
